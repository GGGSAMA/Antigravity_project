# 00099tests/sandbox: 沙盒与临时验证脚本

## 设计愿景
专治各种临时脚本！在开发过程中，如果有需要一次性跑测的代码（比如临时打印一下骨骼信息、验证某个模型加载），统统往这里扔，不要污染项目根目录。

## 架构规约
这里的文件没有严格的命名和代码规范要求，可以随意折腾。只要不被 `0000core` 或 `00041scenes` 等正式逻辑引用即可。
当这里的临时脚本被验证稳定并具备通用价值后，可以将其提升为 `00099tests/` 下的正式单元测试用例。
