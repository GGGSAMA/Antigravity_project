# 游戏项目上下文与开发进度 (AI Context)

**致新开启的 AI 助手**：
当您进入新的对话时，请先阅读本文件以了解当前项目的开发进度、已有系统和接下来的计划。

## 项目概览
- **项目名称**：3D Godot 游戏开发 (修仙/奇幻背景)
- **引擎版本**：Godot 4.x
- **核心机制**：角色移动与控制、大世界地形生成、时间/环境切换、随身秘境。

## 已经完成的核心功能
1. **大地图生成 (`world_generator.gd`)**：
   - 使用 `Terrain3D` 插件生成地形。
   - 使用 `FastNoiseLite` 的 FBM 模式生成了平原与山脉交错的地形（替代了之前太尖锐像剑锋一样的 Ridged 噪声）。
   - 当前生成区域为了防止卡死，限制在了 `2048` 大小（之前8192导致线程卡死）。
2. **环境与光照 (`environments/`)**：
   - 支持天空盒与环境的保存。
   - 之前制作了夜晚月光环境（记录在 `night_env.tres` 等文件中），目前正在调试恢复白天的环境 `day_env.tres`。
3. **随身小秘境双向传送 (`game_root.gd`)**：
   - 按下 **F3** 可以将玩家从大世界瞬间传送到天空中（Y=50000）的 `mystic_realm_room.tscn`（悬浮房间），再按 F3 原路返回。
4. **NPC与实体 (`entities/npc/`)**：
   - 修复了因为 `CharacterBody3D` 缺少 `is_passive` 属性导致报错的 bug，现在 NPC 能正常在 `game_root` 中作为测试单位生成。
5. **初始化场景 (`game_root.tscn`)**：
   - 现在项目启动的默认主场景是 `game_root.tscn`，它负责动态加载 `main.tscn` 并生成玩家与测试 NPC，避免直接改动巨大的地图文件。

## 待办与计划 (TODO)
1. **恢复白天环境**：之前调了夜景，需要确保进入大世界时使用的是白天环境 `day_env.tres` 或 `env_day.tres`。
2. **扩展修仙系统**：
   - 炼丹炉UI与功能逻辑（UI概念图已在旧对话生成）。
   - 突破与打坐系统。
   - 秘境的进一步完善（目前只是个空房间，容易报错，需要改用代码动态生成或者精简节点）。
3. **地形打磨**：
   - 大世界长宽需达到 10000。
   - 从中心 `500, 500` 向外生成。
   - 高度再翻3倍，位置可以再远一点。

## 新对话开启指南
1. 游戏代码根目录在 `e:\0GD\3d-game-in-godot-main\3d_game`。
2. 每次回复请务必在结尾加上 **"牢大~"**。
3. 之前遇到因为生成含有复杂节点的 `.tscn` 文件而导致 AI 崩溃的情况，今后尽量使用 `GDScript` 动态创建节点，或让用户手动建节点 AI 只写代码。
