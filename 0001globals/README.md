# 0001globals: 全局单例与自动加载 (Autoloads)

## 设计愿景
收纳在整个游戏生命周期中永远存活的管理器 (Singleton)。

## 核心组件
- `time_manager.gd`: 负责同步引擎的物理/渲染帧与游戏内的“修仙年月”。
- *(未来可扩展的其他轻量级全局状态)*

## 架构规约
请极力控制这里的规模。非绝对必要（跨越多个场景长期存在的管理类），不要将普通脚本做成全局单例，避免“面条代码”和耦合灾难。
